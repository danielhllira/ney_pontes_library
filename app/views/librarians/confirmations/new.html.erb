<div class="row justify-content-center">
  <div class="col-md-6">
    <div class="card mt-4">
      <div class="card-body">
        <h2 class="card-title mb-4">Reenviar instruções de confirmação</h2>

        <%= form_for(resource, as: resource_name, url: confirmation_path(resource_name), html: { method: :post }) do |f| %>
          <%= render "librarians/shared/error_messages", resource: resource %>

          <div class="mb-3">
            <%= f.label :email, "E-mail", class: "form-label" %>
            <%= f.email_field :email, autofocus: true, autocomplete: "email", value: (resource.pending_reconfirmation? ? resource.unconfirmed_email : resource.email), class: "form-control" %>
          </div>

          <div class="mb-3">
            <%= f.submit "Reenviar instruções de confirmação", class: "btn btn-primary" %>
          </div>
        <% end %>

        <%= render "librarians/shared/links" %>
      </div>
    </div>
  </div>
</div>
