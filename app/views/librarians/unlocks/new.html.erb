<div class="row justify-content-center">
  <div class="col-md-6">
    <div class="card mt-4">
      <div class="card-body">
        <h2 class="card-title mb-4">Reenviar instruções de desbloqueio</h2>

        <%= form_for(resource, as: resource_name, url: unlock_path(resource_name), html: { method: :post }) do |f| %>
          <%= render "librarians/shared/error_messages", resource: resource %>

          <div class="mb-3">
            <%= f.label :email, "E-mail", class: "form-label" %>
            <%= f.email_field :email, autofocus: true, autocomplete: "email", class: "form-control" %>
          </div>

          <div class="mb-3">
            <%= f.submit "Reenviar instruções de desbloqueio", class: "btn btn-primary" %>
          </div>
        <% end %>

        <%= render "librarians/shared/links" %>
      </div>
    </div>
  </div>
</div>
